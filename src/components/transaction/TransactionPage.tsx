import { Box, Typography } from "@mui/material";
import React from "react";

const TransactionPage: React.FC = () => {
  return (
    <Box sx={{ p: 2 }}>
      <Typography variant="h1">Transaction Page</Typography>
    </Box>
  );
};

export default TransactionPage;
